from turtle import*

speed(0)
delay(0)

LENGTH = 10

def rectangle(height, width):
    for i in range(0, 2):
        fd(width)
        lt(90)
        fd(height)
        lt(90)

def main(kod, message):
    n = len(message)

    lista = [0]
    for i in range(0, )
    
    for i in range(0, n):
        rectangle(, (i + 1) * 10)
        fd((i + 1) * 10)

main()
